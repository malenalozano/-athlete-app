# Archivos temporales de Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Entornos virtuales (por si acaso creas uno luego)
venv/
env/

# Bases de datos locales (nunca subir — usamos Turso en producción)
atleta.db
athlete.db
*.db

# Archivos de build frontend
Figma/dist/

# Logs
*.log

# Archivos de configuración de editores
.vscode/
.idea/.aider*
.aider*
# Mis secretos (¡NUNCA SUBIR!)
.env
.streamlit/secrets.toml
src/core/.encryption_key

# Claude Code config local (no subir — cada dev tiene el suyo)
.claude/settings.local.json
